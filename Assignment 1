#include <stdio.h>
#include <string.h>
int main(voi
	int i,s,temp;
	char str[10];
	d)
{scanf("%s",str);
	s=strlen(str);
	for(i=0;i<s;i+=2)
	{
                        if (str[i+1]!='\0')
                        {
		
		temp=str[i];
		str[i]=str[i+1];
		str[i+1]=temp;
                        }
	}
	
		printf("%s",str);
		
		
	
        
	
	return 0;
}
